{% extends 'base.html.twig' %}

{% block body %}
    <h1 class="text-lg self-center">
        {# {{ "hello_world"|trans }} #}
        {% for item in items %}
            <div class="bg-gray-600 rounded-lg p-4">
                <h3 class="text-3xl font-bold">
                    {{ item.name }}
                </h3>
                <p>
                    {{ item.description }}
                </p>
            </div>
        {% endfor %}
    </h1>
{% endblock %}
